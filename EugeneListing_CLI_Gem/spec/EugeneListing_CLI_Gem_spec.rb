RSpec.describe EugeneListingCLIGem do
  it "has a version number" do
    expect(EugeneListingCLIGem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
